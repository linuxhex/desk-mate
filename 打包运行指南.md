# 打包和运行指南

## 方法一：直接运行（开发模式）

如果您只是想快速测试应用，可以直接运行开发模式：

1. 确保已安装Node.js
2. 在项目目录下运行：
   ```bash
   npm install
   npm start
   ```

## 方法二：打包为可执行文件（推荐）

### 步骤1：安装依赖

```bash
npm install
```

### 步骤2：打包应用

#### Windows系统

```bash
npm run build:win
```

这将生成两个可执行文件：
- `dist/桌面Excel分析助手 Setup.exe` - 安装版（需要安装后使用）
- `dist/桌面Excel分析助手.exe` - 便携版（双击即可运行）

#### macOS系统

```bash
npm run build:mac
```

生成 `dist/桌面Excel分析助手.dmg` 文件。

#### Linux系统

```bash
npm run build:linux
```

生成 `dist/桌面Excel分析助手.AppImage` 文件。

### 步骤3：运行应用

#### 便携版
直接双击 `dist/桌面Excel分析助手.exe` 文件即可运行。

#### 安装版
1. 双击 `dist/桌面Excel分析助手 Setup.exe` 文件
2. 按照安装向导完成安装
3. 从开始菜单或桌面快捷方式启动应用

## 方法三：使用electron-packager（备选方案）

如果electron-builder遇到问题，可以使用electron-packager：

1. 安装electron-packager：
   ```bash
   npm install electron-packager --save-dev
   ```

2. 运行打包命令：
   ```bash
   npx electron-packager . "桌面Excel分析助手" --platform=win32 --arch=x64 --out=dist --overwrite
   ```

3. 运行生成的可执行文件：
   ```
   dist/桌面Excel分析助手-win32-x64/桌面Excel分析助手.exe
   ```

## 常见问题

### 1. 打包失败
- 确保已安装所有依赖：`npm install`
- 确保网络连接正常（打包过程需要下载一些依赖）
- 尝试使用管理员权限运行命令行

### 2. 应用无法启动
- 检查是否安装了Node.js（开发模式）
- 检查打包后的文件是否完整
- 查看应用目录下的日志文件（如果有）

### 3. 找不到可执行文件
- 打包完成后，可执行文件位于 `dist` 目录下
- 便携版文件名为 `桌面Excel分析助手.exe`
- 安装版需要先安装才能使用

## 技术说明

### 打包工具
- **electron-builder**：功能强大的打包工具，支持生成安装包和便携版
- **electron-packager**：简单的打包工具，只生成便携版

### 打包配置
- 配置文件：`package.json` 中的 `build` 部分
- 输出目录：`dist`
- 支持的平台：Windows、macOS、Linux

### 运行环境
- 便携版：无需安装，双击即可运行
- 安装版：需要安装后运行，会创建桌面快捷方式和开始菜单条目

## 总结

1. **快速测试**：使用 `npm start` 直接运行
2. **正式使用**：使用 `npm run build:win` 打包为可执行文件
3. **推荐**：使用便携版 `桌面Excel分析助手.exe`，双击即可运行，无需安装
